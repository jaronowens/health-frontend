// Constants that represent the endpoints used across the application. A change to these will change the endpoints throughout.
const BASE_URL = 'http://localhost:8080';

const CONTEXT_PATIENTS = '/patients';
const CONTEXT_ENCOUNTERS = '/encounters';

export { BASE_URL, CONTEXT_ENCOUNTERS, CONTEXT_PATIENTS };